#!/usr/bin/env bash
# Lists filenames in the current directory, displaying only the part after the first dash

for file in *; do
  # Check if it’s a file or directory and contains a dash
  if [[ "$file" == *-* ]]; then
    # Extract and print substring after the first dash
    echo "${file#*-}"
  fi
done
