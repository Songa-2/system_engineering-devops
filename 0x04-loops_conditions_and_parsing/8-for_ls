#!/usr/bin/env bash
# Lists non-hidden files, printing only the part after the first dash

for file in *; do
  # Skip directories and hidden files
  if [[ -f "$file" && "$file" == *-* ]]; then
    echo "${file#*-}"
  fi
done
